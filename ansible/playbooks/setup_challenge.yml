---
- name: Setup challenge VM
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Clone challenge repository
      ansible.builtin.git:
        repo: "{{ challenge_repo_url }}"
        dest: /opt/challenge
        version: master