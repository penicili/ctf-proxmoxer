---
- name: get all associated vms
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Get all VMs
      community.proxmox.proxmox_kvm:
        api_user: "{{ proxmox_api_user }}"
        api_password: "{{ proxmox_api_password }}"
        api_host: "{{ proxmox_api_host }}"
        api_port: "{{ proxmox_api_port }}"
        validate_certs: false
        vmid: all
      register: all_vms